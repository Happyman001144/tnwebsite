{% extends 'templates/app.twig' %}

{% block title %}{{ 'impressum'|lang }}{% endblock %}

{% block content %}
  <div class="page-header">
    <h1 class="title container">{{ block('title') }}</h1>
  </div>
  <div class="container">
    <p>{{ setting.impressum.value|raw }}</p>
  </div>
{% endblock %}
